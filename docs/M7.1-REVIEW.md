# M7.1 Reviewer pass

**Date:** Wed 9 Sep 2026  
**Reviewer:** T1 · card `M7.1`  
**Base:** `origin/main` `ddbaad09f4a0af176339f7181b0ccceb42c2e753` (`docs: M5.7 mayor panel brief for FE`)  
**Scope:** AGENT-RUNBOOK §4 on current main (M0–M4 done, M5.1+M5.4, M6 live 12-ticks, M7.2 README, M7.3 SUBMISSION). Sample, not an exhaustive audit.  
**Not in scope:** `apps/web` edits, M5.7/M5.2 implementation, stretch B/D, live 12-tick, loan approve, `--yes`/`--broadcast`, `.env`.

No P0 found. No code changed in this PR (findings-only).

---

## AGENT-RUNBOOK §4 checklist

- [x] **Exit criteria in brief are met with evidence (test output, tx hash, screenshot).**  
  This card’s exit is “checklist in AGENT-RUNBOOK all ticked.” Items below are ticked or called out with file:line. Live evidence already on main (STATUS): treasury `0xCE0e…FfC1`, registrar `0xe4A1…0f7F`, 12-tick `execute=LIVE` with 0 failed txs after #40, CI green (run `34426411806` on `ddbaad0`, 2m11s). Remaining demo-path gaps are P1 (mayor UI in flight; fixture ids vs live — known).

- [x] **No secrets, private keys, entity secrets, or API keys in diff; `.env.example` updated if new var.**  
  `git ls-files` tracks `.env.example` only (`.gitignore` blocks `.env` / `.env.*`). Grep: no `BEGIN PRIVATE KEY`, no filled `CIRCLE_ENTITY_SECRET`/`CIRCLE_API_KEY`/`ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/`DEPLOYER_PRIVATE_KEY`/`ENS_TREASURER_PRIVATE_KEY`/`GRAPH_API_KEY`/`SUPABASE_SERVICE_KEY`. `.env.example` lists vars; secrets are empty. `packages/circle/roster.json` wallet ids + `CIRCLE_WALLET_SET_ID` are operational identifiers (need Console API key + entity secret to act). STATUS records Studio query URL `…/query/1758998/agent-town/0.0.1` **without** a key — not a secret. This PR adds docs only.

- [x] **Solidity: CEI, `SafeERC20`/return checks, access control on every state-changing fn, events for every state change, 18-decimal USDC assumptions explicit, no `address(0)` transfers.**  
  `TownTreasury.sol`: `SafeERC20`, `nonReentrant` on token movers, treasurer/owner/`TREASURER_ROLE` on admin, events on every mutation, `decimals()` at deploy (`usdcDecimals` immutable), `payStipend`/`registerAgent`/constructor reject `address(0)`. CEI: accounting + emit then `safeTransfer*`. `foundry.toml` `evm_version = "paris"`. Arc USDC is **6** dec ERC-20 vs **18** native (`ArcConfig.sol:16-19`, `packages/shared/src/constants.ts:4-26`) — runbook still says “18-decimal USDC”; code follows RISKS R4 (P2-01). `TownRegistrar.sol`: `onlyOwner` on `register`/`renew`, `ZeroAddress`, no stored tokenIds.

- [x] **ENS: tokenIds never cached; role checks tested negatively; hackathon deployment addresses only.**  
  `packages/ens/src/client.ts:145-157` re-reads `getState(labelhash)` before writes; `revokeName` uses live `state.tokenId` in-call only (`:287-294`). `town.json` addresses only. Scripts print tokenIds, do not persist (R3). Negative: `TownRegistrar.t.sol:57` `test_WorkerCannotSetCreditScore_TreasurerCan`. `deployments.json` `deployment: ensv2-hackathon-frozen` (2026-09-03), not main beta.

- [x] **Circle: transactions polled to terminal state; failures surfaced, not swallowed.**  
  `packages/circle/src/execute.ts:179-200` `waitComplete` → COMPLETE or `CircleTxFailed` / `CircleTxTimeout`. Sim `execute.ts:452-457` maps those to `failed`/`pending`; `engine.ts:87-88` `console.error`. Mayor `apps/api/src/real/mayor.ts:59-79` awaits `waitComplete` (throws if no txHash). Client never logs `CIRCLE_API_KEY` / `CIRCLE_ENTITY_SECRET` (`client.ts:4-5`).

- [x] **Graph: no mocked/static data on any production path; queries typed.**  
  `API_MODE=mock` → `MockStore` + `FIXTURES`. Real path: `index.ts:21-26` → `RealSource` (subgraph + ENS + ERC-20 balances). `RealSource` does not import `FIXTURES`. `createGraphClient` requires `SUBGRAPH_URL` (never hardcoded). Queries via codegen SDK + zod (`packages/graphclient/src/queries/scoreboard.ts:28`, `schemas.ts`). Signal C uses gateway + last-known `stale=true`, not town fixtures.

- [x] **Rules: deterministic; LLM paths have timeout + fallback + caps.**  
  `apps/sim/src/decide.ts` pure (no Date/network/LLM). Advisor: `LLM_TIMEOUT_MS=8000`, abort + rules fallback (`advisor.ts:130-148`, `:221-223`), `maxAmount` clamped, rules `flag`/`deny` cannot be auto-approved (`:82-98`). Narrator: same timeout + static fallback. Signal C: `EXTERNAL_SIGNAL_TIMEOUT_MS=5000` + last-known (`fetchExternalSignals.ts:55-98`). Treasurer caps: score ≥ 60, util < 80% (`decide.ts:192-217`).

- [x] **Readability: module has a 3–6 line header comment (purpose, inputs, outputs); functions ≤ 60 lines; names say what not how; no commented-out code.**  
  Key BE modules have purpose/inputs/outputs headers. No commented-out blocks found in sampled src. **Nits (P2):** several functions ≫ 60 lines (`executeProposedAction`, `RealSource.refresh`, `patchDemoWorld`); `mayor.ts` header is one line; `notImplementedSource` leftover after M4.7.

- [x] **Types: zod at boundaries; no `any` without comment.**  
  HTTP: `apps/api/src/routes.ts:38-101` zod-parses every request body/query and every JSON response. Web client also parses (`apps/web/lib/api.ts`). Hand-written TS: no `: any` / `as any`. Only `variables?: any` in codegen `packages/graphclient/src/generated/graphql.ts:561`. Circle transfer uses `as never` with a comment (`execute.ts:140-151`).

- [x] **Tests added/updated for the card's logic; `pnpm -r build && pnpm -r test` green.**  
  No production logic in this card. Evidence: GitHub Actions **CI success** on `origin/main` `ddbaad0` — `pnpm -r --if-present build/test/lint` (workflow `.github/workflows/ci.yml`, run `34426411806`, 2026-09-10T01:40Z). Foundry via tarball `v1.8.1` (known CDN outage). Did not re-run locally (timebox; no `--yes`).

- [x] **Docs touched if behaviour or env changed.**  
  Behaviour/env unchanged. This file is the M7.1 artefact. `.env.example` already lists required vars with empty secrets.

---

## Findings

| ID | Sev | Area | file:line | Note | Owner |
|---|---|---|---|---|---|
| F-P1-01 | **P1** | Demo path (PRD §8 mayor click) | `apps/web/components/Shell.tsx:125-129` | Bank + feed are wired; **no Mayor panel**. `lib/api.ts:80-88` already has typed POSTs. Judge cannot click Approve → toast → arcscan. In flight: `docs/M5.7-MAYOR-BRIEF.md` (Dan). Do not start here. | FE |
| F-P1-02 | **P1** | Demo path (PRD §12 storyline vs live) | `apps/sim/src/storyline-hooks.ts:34-37`; `apps/sim/src/execute.ts:88-100` | Demo still keys repay/default/job-settle to fixture ids `L-1` / `L-2` / `L-flag` / `J-demo`. Live execute **skips** non-numeric ids (intentional after #38/#40 — avoids failed Circle txs). Live 12-tick therefore does not land PRD repay/default/job-settle on chain. **Already known** (STATUS 17:45 / #40); not a new P0. | BE |
| F-P1-03 | **P1** | Real API rate tooltip (PRD §12) | `apps/api/src/real/store.ts:223-237` | `spreadBps = max(0, on-chain baseRate − marketApy)`. After `set_rate` to `computeTownRateBps` (market+spread+premium), that difference **already includes** default premium. `townRateBps` then adds `DEFAULT_PREMIUM_BPS` again when `defaults > 0` → e.g. live base 839 bps would tooltip as **1039**. Mock path uses fixture spread 200 and is fine. Hits at M5.8 real swap. Fix: use `BASE_RATE_SPREAD_BPS` (or subgraph spread), not `base − market`. | BE |
| F-P2-01 | P2 | Docs / Solidity checklist wording | `docs/AGENT-RUNBOOK.md:44` | Checklist still says “18-decimal USDC assumptions explicit”. R4 + code are 6-dec ERC-20 / 18-dec native. | BE |
| F-P2-02 | P2 | Dead code | `apps/api/src/source.ts:47-64` | `notImplementedSource()` leftover (“real mode lands in M4.7”). `index.ts` uses `RealSource`. Unused. | BE |
| F-P2-03 | P2 | Readability | `apps/sim/src/execute.ts:180`; `apps/api/src/real/store.ts:191`; `apps/sim/src/storyline-hooks.ts:107` | `executeProposedAction` / `refresh` / `patchDemoWorld` well over 60 lines. Headers otherwise OK. | BE |
| F-P2-04 | P2 | Headers | `apps/api/src/real/mayor.ts:1` | One-line header; runbook wants 3–6 (purpose, inputs, outputs). | BE |
| F-P2-05 | P2 | ENS explorer link | `apps/api/src/real/store.ts:343` | Real agent `links.ens` → `sepolia.app.ens.domains` (v1). FE STATUS already noted ENSv2 explorer is `https://explorer.ens.dev/`. | FE |
| F-P2-06 | P2 | Map polish | `apps/web/components/MapSlot.tsx:1-4` | SVG placeholder (4 zones + positions). Pixi map is M5.2 (Astra, in flight). Not a demo-break. Do not start here. | FE |
| F-P2-07 | P2 | Types | `packages/graphclient/src/generated/graphql.ts:561` | Codegen `variables?: any`. Do not hand-edit; regenerate later. | BE |
| F-P2-08 | P2 | CORS | `apps/api/src/routes.ts:57` | Reflects any `Origin` (comment says localhost iterate). Fine for mock; tighten before a public real deploy. | BE |

---

## Already known (not re-filed as P0)

| Item | Where |
|---|---|
| Live 12-tick skips `L-1` / `J-demo` fixture ids | STATUS Wed 9 Sep 17:45; #38 `0d4a76e`, #40 `7220a16`. Same as F-P1-02. |
| Loans **#7 / #8** left Pending on purpose | STATUS 17:45. Do not `approve` from this card. Useful once F-P1-01 ships. |
| foundryup attestation CDN down; CI uses GitHub tarball **v1.8.1** | `.github/workflows/ci.yml:30-35`; #44 `a78847f`. CI green. |
| Circle faucet **403** | STATUS 16:45. Blocks clean re-seed, not this review. |

---

## Sample notes (PASS, not findings)

- **Treasury CEI / AC:** `deposit`/`withdraw`/`approveLoan`/`repay`/`fund`/`payStipend` — effects then `SafeERC20`; `approveLoan`/`denyLoan`/`set*` = `onlyTreasurerOrOwner`; `markDefault`/`payStipend` = `TREASURER_ROLE`; `requestLoan` public by design.
- **Decimals:** `TownTreasury` reads `IERC20Metadata.decimals()`; Arc scripts use `0x3600…0000` 6 dec; Circle `transferUsdc` comment forbids native 18-dec (`execute.ts:131-134`).
- **Paris:** `foundry.toml:12`; no PUSH0 on Arc bytecode path.
- **Graph production:** `subgraph.yaml` `network: arc-testnet`, treasury `0xCE0ed3b88F60EefB8EA77D1daeC5cEE3a9e4FfC1` startBlock `61140537`. Roster-filtered scoreboard (`scoreboard.ts:12-17`).
- **Advisor guards:** flag-wins + USDC cap tested in sim (STATUS: PR #31 77/77).

---

## Verdict

**M7.1 reviewer pass: COMPLETE (findings-only).**  
P0 = **0**. P1 = **3** (mayor UI in flight; known fixture-id skip; real-API rate double-count). No BE/docs P0 one-liner to fix. Do not merge this as “demo ready” — F-P1-01 is the PRD §8 gate (Dan / M5.7).
