# M7.1c — leftover P2 nits (BE only)

```
CARD: M7.1c leftover P2 nits from M7.1
TIER: T3   OWNER: BE
CONTEXT: docs/M7.1-REVIEW.md findings F-P2-01, F-P2-02, F-P2-04
INPUTS: this brief; AGENT-RUNBOOK §4; apps/api/src/source.ts; apps/api/src/real/mayor.ts
OUTPUT: PR with the three items below + tests still green
EXIT: listed files changed; `pnpm --filter @agent-town/api test` green; no apps/web
TIMEBOX: 1h. Report leftover, do not widen.
DO NOT: touch apps/web, packages/shared, ALLOW_BROADCAST, loans #7/#8, stretch B/D, live ticks, F-P2-03 splits, F-P2-07 codegen, F-P2-08 CORS
```

Branch: `card/m7.1c-p2-nits` off `origin/main`. Worktree `/tmp/wt-m7.1c`.

## Do these three only

1. **F-P2-01** `docs/AGENT-RUNBOOK.md` Solidity checklist still says “18-decimal USDC assumptions explicit”. Code + RISKS R4 are **6-dec ERC-20 USDC / 18-dec native gas**. Fix the wording. Do not change contracts.
2. **F-P2-02** Delete unused `notImplementedSource()` in `apps/api/src/source.ts` (and any import/export). `index.ts` already uses `RealSource`. Confirm nothing imports it before delete. Add/adjust a test only if one asserted on it.
3. **F-P2-04** Expand `apps/api/src/real/mayor.ts` header to 3–6 lines (purpose, inputs, outputs). No behaviour change.

## Skip (explicit)

- F-P2-03 function splits (`executeProposedAction` / `refresh` / `patchDemoWorld`) — out of timebox.
- F-P2-05 ENS explorer — FE #48 landed; do not change API `links.ens`.
- F-P2-06 map — Dan / M5.2.
- F-P2-07 `variables?: any` — codegen; do not hand-edit.
- F-P2-08 CORS — leave for a public deploy, not this hackathon.

## PR body must include

what/why, how to test (`pnpm --filter @agent-town/api test` + which files), confirmation: no `apps/web`, no shared freeze break, no `ALLOW_BROADCAST`, no loan approve.
