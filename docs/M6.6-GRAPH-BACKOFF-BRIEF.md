# M6.6 — Studio 429 backoff + ledger-independent refresh

P0 for Sat film. Real API has been 429-spamming Graph Studio since Fri 03:50 UTC. Quota **resets Fri 15:22 EDT**. Do **not** edit `apps/api` on the primary checkout (`tsx --watch` would restart and drop the only warm cache).

```
CARD: M6.6 Graph 429 backoff
TIER: T1   OWNER: BE (Kenny)
CONTEXT: this file; apps/api/src/real/store.ts refresh(); DAN-LIVE-SPLIT
INPUTS: running API PID on :3001 (leave it); Studio slug agent-town
OUTPUT: worktree /tmp/wt-m6.6 · branch card/M6.6-graph-backoff
EXIT: refresh keeps last-good subgraph on 429; ledger tick/narration still applied; backoff honors Retry-After; tests for 429 + stale-keep; no primary-checkout edits
TIMEBOX: 2h
DO NOT: --ticks 12 --yes; markDefault bo; restart :3001 until quota reset; touch apps/web; packages/shared; MapSlotProps
```

Repo: https://github.com/kenjohnscreates/agent-economy  
Branch: `card/M6.6-graph-backoff` off **current `origin/main`**.  
PR title: `M6.6: Studio 429 backoff; ledger refresh without graph`

`touching: apps/api/src/real/store.ts apps/api/src/real/sse.ts`

## Why

`:3001` `pnpm --filter @agent-town/api dev` polls every `TICK_MS` (15s). Each refresh hits Studio (scoreboard + loans + gdp + 8× `agentState` jobs). Burned `x-ratelimit-limit: 3000`. Latest error: remaining 0, reset **2026-09-11 15:22 EDT**.

`refresh()` reads Supabase **then** graph. Graph throw aborts the whole refresh → cache frozen at boot (tick 0, empty narration) even after sim wrote tick 1.

Boot cache still serves the live UI (header `api · real`, mayor **#11**, rate 8.29%). **Leave that process up.** A `--watch` restart now → 501 until 15:22.

## Do

1. Worktree `/tmp/wt-m6.6`. Never save api files in the primary repo while :3001 `--watch` is running.
2. On Studio/graph error (esp. 429): keep last-good scoreboard/loans/jobs; still apply ledger tick/actions/narration; set `signals.stale=true` if Signal C also failed.
3. Backoff: honor `Retry-After` / `x-ratelimit-reset`; stop 15s hammer while remaining=0.
4. Optional: one roster jobs query instead of 8× `AgentJobs`.
5. Tests: 429 keeps prior scoreboard; ledger-only update still emits tick/narration SSE; backoff skips graph until reset.

## After merge

Wait until **15:22 EDT** (or Retry-After). Then restart:

```
pnpm --filter @agent-town/api dev
```

Expect `/health` `tick:1` and 8 narration chips. Do not `--yes`.

## Not this card

Loan book showing `0xc0469ad2…` instead of `cy` (subgraph `ensName` null; roster addr is cy). Follow-up **M6.6b** if time: `mapLoan` fallback via roster address. Do not dual-write BankPanel/MayorPanel.
