# M9.1 — Circle Gateway (Stretch B)

Authorized Fri. Timebox ~5h. **Drop Sat if faucet/API blocks.** Human gate still: **new Sepolia Circle wallet** — do not `setup-wallets --yes` until Kenny says approve.

```
CARD: M9.1 Circle Gateway
TIER: T1   OWNER: BE (Kenny)
CONTEXT: PRD §3 B; MILESTONES M9.1; DAN-LIVE-SPLIT bank inbound strip
INPUTS: Circle set 949545dc-5e02-5050-8e2f-7e6bc12bfed3; gus Arc SCA already exists
OUTPUT: gus ETH-SEPOLIA SCA + Gateway deposit path → TownTreasury / bank inbound UI
EXIT: one Sepolia USDC (Circle `0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238`, not ENS MockUSDC) lands as Arc USDC visible in bank inbound; tx links
TIMEBOX: 5h Friday. Stop if faucet 403 or Gateway API blocks.
DO NOT: Stretch D; yield vault A; Circle keys to Dan; DEPLOYER_PRIVATE_KEY in git/FE; --ticks 12 --yes; markDefault bo; dual-write BankPanel while Dan has a web PR open
```

Visitor = **gus**. Token = Circle Sepolia USDC `0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238`.

## Sequence (after wallet gate)

1. Worktree `/tmp/wt-m9.1` · `card/M9.1-gateway`.
2. Create **gus** SCA on `ETH-SEPOLIA` in the existing wallet set (new address, do not reuse Arc gus).
3. Fund that SCA with Circle Sepolia USDC (faucet). If 403, **stop** and film Arc-only.
4. Gateway deposit → unified balance → settle on Arc into TownTreasury (or ada/bank inbound).
5. Bank panel **inbound** strip only if Dan is not mid-PR on `apps/web` — ping `touching:` in STATUS. Prefer a thin API field + existing BankPanel slot over rewriting the panel.

## Frozen

`packages/shared` schemas need both humans if you add a field. Mock must update in the same PR.

## Not started until

Kenny: `approve M9.1 wallets` (new Sepolia Circle wallet).

## Gate

**Approved Fri 11 Sep 12:24 EDT** — `approve M9.1 wallets`. Create **gus only** on `ETH-SEPOLIA` in set `949545dc-5e02-5050-8e2f-7e6bc12bfed3`. Do **not** `setup-wallets --yes` (that path is Arc-only and would skip gus).
