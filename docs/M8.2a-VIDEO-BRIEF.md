# M8.2a — tighten demo video outline (docs only)

**SUPERSEDED Fri 11 Sep.** Prize video = **Sat 12 Sep live** (`API_MODE=real`, mayor **#11**), not mock. Canonical notes: [`docs/SUBMISSION.md`](SUBMISSION.md). This card’s mock-film instructions are historical.

```
CARD: M8.2a video outline tightening
TIER: T3   OWNER: BE/docs
CONTEXT: docs/PRD.md §10 + §12; docs/SUBMISSION.md Demo video outline; docs/STATUS.md (M5.8 on main, M5.2 map missing)
INPUTS: this brief
OUTPUT: docs/SUBMISSION.md outline that a human can film Sunday without guessing
EXIT: table + filming notes; no apps/* code
TIMEBOX: 45m
DO NOT: record footage; edit apps/web; set ALLOW_BROADCAST; approve #7/#8; stretch B/D; live 12-tick; change prize blurbs except if a link is already stale vs STATUS
```

Branch: `card/m8.2a-video-outline` off `origin/main`. Worktree `/tmp/wt-m8.2a`.

## Replace the placeholder in `docs/SUBMISSION.md` § Demo video outline

Keep the PRD §10 beat times as the **target** table. Add a **filming notes** subsection that is honest about what is on main **now** (Thu 10 Sep):

1. Record against **mock** (`API_MODE=mock`, `pnpm --filter @agent-town/api dev:mock` + web `:3000`). Real is opt-in; mayor POSTs **501** without `ALLOW_BROADCAST=true`. Do not enable broadcast in this card.
2. **M5.2 map is still a placeholder** (no PR). If map has not landed by record time: hero = bank panel + mayor panel + agent cards + feed, not the overworld. If M5.2 has merged by then, use the map as hero (PRD §12).
3. **Mayor click (PRD §8) works on mock** (M5.7 #47). Toast + feed. Do **not** approve on-chain loans **#7/#8** unless the human says so.
4. Live 12-tick does **not** land repay/default/job-settle (fixture ids `L-1`/`J-demo` skipped — #38/#40). Do not promise those beats on arcscan from a live run. Mock storyline still plays §12 for the recording.
5. Agent cards (M5.8 #48): arcscan wallet + ENS explorer links. `ada.botanica.eth`. Connection warming card exists for real 501; not needed if filming mock.
6. Architecture slide: ENS Sepolia names → Arc USDC + TownTreasury `0xCE0e…FfC1` → Studio subgraph `agent-town` → Signal C Aave/Uni. Town = **botanica**. Do not paste `SUBGRAPH_URL` (local `.env` only).

Do not invent tx hashes. Do not add a video URL (still TBD). Commit + PR.
