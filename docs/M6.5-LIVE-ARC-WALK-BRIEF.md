# M6.5 — live Arc walk (Gate A)

**DONE Thu 10 Sep 23:30.** cy **#9 Defaulted**; bo **#10 Repaid**; **#11 Pending** for mayor. Do not re-run. Follow-up: deliver fix `a475ccb`; Supabase + real API Fri 00:05.

```
CARD: M6.5 live Arc walk
TIER: T1   OWNER: BE
CONTEXT: live demo plan; docs/DAN-LIVE-SPLIT.md; M6.4 #56 on main
INPUTS: TownTreasury 0xCE0e…FfC1; cy SCA; ALLOW_BROADCAST
OUTPUT: cy Active loan past grace; live `--ticks 12 --yes`; one pending loan for mayor; API_MODE=real
EXIT: arcscan txs for buys + t7 markDefault **cy** (not bo); UI against real API
TIMEBOX: tonight
DO NOT: markDefault bo; revokeName; stretch D; ENS MockUSDC as payment
```

## Seed

`cy` `requestLoan` (~0.2 USDC, term **1s**) → ada `approveLoan` → wait `dueAt + gracePeriodSeconds` (~120s). Do not default **bo**.

## Live 12-tick

```
ALLOW_BROADCAST=true STORYLINE=demo TICK_MS=15000 \
  pnpm --filter @agent-town/sim tick -- --ticks 12 --yes
```

M6.4 holds repay until t9 so t7 can `markDefault` the Active **cy** loan.

## After

Leave a **Pending** flagged loan (low-score borrower) for the mayor click. Flip local `.env` `API_MODE=real`.
