// @agent-town/sim — tick engine entry point (ARCHITECTURE §6.1).
// Currently a smoke-test stub proving the workspace link to @agent-town/shared.
// Role rules, advisor and narrator arrive in later milestone cards.
import { ARC_USDC_ADDRESS } from "@agent-town/shared";

console.log(`[sim] booting — Arc USDC (ERC-20, 6 dec): ${ARC_USDC_ADDRESS}`);
