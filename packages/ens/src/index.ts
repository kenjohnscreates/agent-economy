// @agent-town/ens — ENSv2 client (resolve, setRecords, role checks) on Sepolia.
// Placeholder export only; real client lands in M0.3+ (viem + UniversalResolverV2).
// Hackathon deployment addresses live in deployments.json (added by card M0.3).
export const PACKAGE = "@agent-town/ens" as const;
