// @agent-town/circle — Circle Developer-Controlled Wallets wrapper
// (create, fund, execute, poll to terminal state). Placeholder export only;
// the execution wrapper is T1 work and lands in a later card.
export const PACKAGE = "@agent-town/circle" as const;
