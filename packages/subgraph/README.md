# @agent-town/subgraph

Placeholder for the Agent Town subgraph (network: Arc Testnet) — `schema.graphql`, `subgraph.yaml`, AssemblyScript mappings. See `docs/ARCHITECTURE.md` §5.
Lands in milestone M3; `codegen`/`build` scripts are stubs until then. Do not run `graph init` here before that card.
