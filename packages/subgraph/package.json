{
  "name": "@agent-town/subgraph",
  "version": "0.0.0",
  "private": true,
  "description": "Agent Town subgraph (Arc Testnet) — schema, manifest, AssemblyScript mappings",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "test": "graph codegen && graph test",
    "deploy:studio": "graph codegen && graph build && graph deploy agent-town --version-label 0.0.1"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.35.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "assemblyscript": "0.19.10",
    "matchstick-as": "0.6.0"
  }
}
