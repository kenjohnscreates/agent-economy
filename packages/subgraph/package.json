{
  "name": "@agent-town/subgraph",
  "version": "0.0.0",
  "private": true,
  "description": "Agent Town subgraph (Arc Testnet) — schema, manifest, AssemblyScript mappings",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.35.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.98.1"
  }
}
